export const metadata = {
  title: "Review Pending Guides | Relayable",
  description:
    "Review guides that are pending approval or require further review.",
};

const ReviewPendingLayout = ({ children }) => {
  return <>{children}</>;
};

export default ReviewPendingLayout;
