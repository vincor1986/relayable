export const metadata = {
  title: "Request New Platform | Relayable",
  description:
    "Request a new platform to be added to Relayable for interactive guides.",
};

const RequestPlatformLayout = ({ children }) => {
  return <>{children}</>;
};

export default RequestPlatformLayout;
